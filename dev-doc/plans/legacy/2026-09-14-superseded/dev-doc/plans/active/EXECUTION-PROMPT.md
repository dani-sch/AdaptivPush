# AdaptivPush execution intake

Start with `AGENTS.md`, [overview](/dev-doc/main/OVERVIEW.md), [plan index](/dev-doc/plans/active/PLAN-INDEX.md), [current state](/dev-doc/main/CURRENT-STATE.md), and the [execution register](/dev-doc/plans/active/ADAPTIVPUSH-EXECUTION-REGISTER.md). The approved decision record owns product decisions; the master owns implementation behavior; code status owns facts; database plan owns schema; traceability owns acceptance.

This consolidation stops for final user review. Do not begin application implementation, database migration or deployment merely because this intake document exists. When subsequent work is authorized, select one bounded AP packet, re-inspect current git/code/environment, preserve user changes, and apply the register's scope, compatibility, free/downgrade/offline/privacy/accessibility and evidence gates.

Immediate future packet is AP-01.1: read-only catalog/grant/ledger/restore evidence assessment. Historical August live results remain dated inputs; current effective catalog grants, full atomic persistence, actual mobile/missing-schema fallback and migration/restore posture are not newly verified. Do not use old stage names or stale execution claims as current authority.

Use a feature branch, small focused local commits, and the actual integration workflow when configured. Never push directly to main, force-push, absorb unrelated user hunks, or claim runtime/security/device/database validation from documentation. Resolve historical source locators through the [archive inventory](/dev-doc/plans/active/ADAPTIVPUSH-DOCUMENT-INVENTORY.md).
